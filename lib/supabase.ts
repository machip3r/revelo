/**
 * Browser-safe Supabase client. For Route Handlers and Server Components, use
 * `lib/supabase/server`; for trusted mutations with service role, use `lib/supabase/admin`.
 */
export { createClient } from "./supabase/client";
